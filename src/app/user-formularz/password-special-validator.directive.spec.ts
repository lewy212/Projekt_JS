import { PasswordSpecialValidatorDirective } from './password-special-validator.directive';

describe('PasswordSpecialValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordSpecialValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
